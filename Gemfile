# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Synchronize with https://pages.github.com/versions
# ruby '>=3'

gem 'jekyll', "~> 4.2"

# gem 'github-pages', group: :jekyll_plugins

gem "webrick", "~> 1.7"

# Resolves sass-embedded-1.57.1-x86_64-linux-gnu requires rubygems version >= 3.3.22, which
# is incompatible with the current version, 3.1.6
# See: https://community.cloudflare.com/t/deployment-failing-rubygems-version/446483/3
gem "jekyll-sass-converter", "~> 2.0"
